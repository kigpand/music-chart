import styles from "./Sort.module.scss";

const Sort = () => {
  return (
    <div className={styles.sort}>
      <button>오름</button>
      <button>내림</button>
    </div>
  );
};

export default Sort;
